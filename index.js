
const app = require ("./app");
const PORT = process.env.PORT || 8000; // Corrected assignment






// Listen on server
app.listen(PORT, () => {
  console.log(`Server is running on port ${PORT}`); // Use backticks for template literals
});
 // <% %> this used for paper code     <% -%> this partial and <% =%> this means you to show in browser
 
 
 